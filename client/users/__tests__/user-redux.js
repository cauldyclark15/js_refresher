/* eslint-env node, jest */
// import userReducer, {DEFAULT_STATE} from '../user-redux'

// test('return initial state', function () {
//   var initialState = userReducer(DEFAULT_STATE, {type: 'TEST_ACTION'})
//   expect(initialState).toMatchObject({
//     isFetching: expect,
//     users: [],
//     error: ''
//   })
// })
